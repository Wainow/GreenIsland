<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorBackgroundToolbar</item>
        <item name="colorPrimaryDark">@color/colorBackgroundDark</item>
        <item name="colorAccent">@color/colorBackgroundItem</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ProgressBarStyle">
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <string name="search_hint">Find company or ticker</string>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Gets rid of the search icon -->
        <item name="searchIcon">@drawable/ic_action_search</item>
        <!-- Gets rid of the "underline" in the text -->
        <item name="queryBackground">@color/colorBackgroundCurrentItem</item>
        <!-- Gets rid of the search icon when the SearchView is expanded -->
        <item name="searchHintIcon">@null</item>
        <!-- The hint text that appears when the user has not typed anything -->
        <item name="queryHint">@string/search_hint</item>
        <item name="android:background">@drawable/bg_black_rounded</item>
    </style>


    <style name="spinnerTheme" parent="Widget.AppCompat.Light.DropDownItem.Spinner">
        <item name="strokeColor">@color/colorBackgroundTab</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>
</resources>